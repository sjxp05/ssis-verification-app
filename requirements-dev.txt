# 임베딩 모델 변환 전용 (tools/export_model.py)
#
# 인터넷이 되는 개발 PC에서 한 번만 실행하고 결과물을 커밋한다.
# 앱 런타임에는 필요 없으므로 exe 번들에 들어가지 않는다.
#
#   pip install -r requirements-dev.txt
#   python tools/export_model.py

--extra-index-url https://download.pytorch.org/whl/cpu

onnx==1.22.0
torch==2.13.0+cpu
transformers==5.14.1
